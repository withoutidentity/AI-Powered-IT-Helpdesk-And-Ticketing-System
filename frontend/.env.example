# Frontend public config. Copy to frontend/.env when you need local overrides.
# Do not put secrets here: Angular bundles these values into browser JavaScript.
NG_APP_API_BASE_URL=http://localhost:5175/api/v1